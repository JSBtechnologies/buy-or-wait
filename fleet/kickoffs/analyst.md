# Kickoff: analyst (worktree, plan mode until approved)

1. Read `fleet/AGENT_RULES.md` and `fleet/kickoffs/_plan_mode.md`.
2. Get your state (rules §5).
3. Read the plan `docs/image_accuracy_plan.md`: §1–§3.

## Phase B work to plan
You own `RULES.md`.

1. **Update RULES.md S5:** the user's final per-image rulings (rules §8), the witness spec per image (plan §3 and the witness map), and the "final printed amount is truth" rule.
2. **Verbatim fixtures:** transcribe every candidate figure and witness on all 16 images exactly as printed. These are dev-only test fixtures, never runtime data.
   - Plan them now.
   - After approval, commit them as a RULES.md section and publish on topic `rules`.
   - ml-engineer needs these early, so if the lead asks, deliver them first.
3. **Then:** stand by for verifier mismatches.
